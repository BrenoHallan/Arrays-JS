//crie uma constante (const) para armazenar valores numericos inteiros ou valores nao inteiros
const valores = [10, 2, 8]

const media = (valores[0] + valores[1] + valores[2]) / valores.length
//                     ^                    ^                     ^
//   posicao do valor no array      nome do array       comprimento do array

console.log(media)